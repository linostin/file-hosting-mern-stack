version: '2.1'
orbs:
  node: circleci/node@4.2.0
jobs:
  test:
    app-dir: ./server
    executor:
      name: node/default
      tag: '15.11'
    steps:
      - checkout
      - node/install-packages
      - run:
          command: npm run test
workflows:
  test_my_app:
    jobs:
      - test